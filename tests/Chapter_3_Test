**Java Review Questions Answers (բացատրություններով)**

1. B,D – Միայն `==` և `!` օպերատորներն են կիրառելի boolean արժեքների վրա։
2. A,C,D – `x + y` արտահայտությունը ունի `int` տիպ, հետևաբար այն կարելի է վերագրել `int`, `long`, կամ `double` տիպերին։
3. B,C,E – `++`, `--`, և `+=` օպերատորները կիրառելի են թվային տիպերի վրա։
4. A – `%` օպերատորը վերադարձնում է բաժանման մնացորդը։
5. B – `short`-ը միակ ճիշտ տիպն է, որը համապատասխանում է տրված միջակայքին։
6. B – `&&` օպերատորը իրականացնում է կարճ միացումով AND գործողություն։
7. B,D,E – `>=`, `!=`, և `==` համեմատական օպերատորներն են թվերի համար։
8. A,E,D – Սինտաքսապես բոլոր արտահայտություններն են ճիշտ։
9. C – `false`-ը համարժեք է 0-ին, ուստի կտպվի 0։
10.F – `!=` օպերատորը կարելի է օգտագործել boolean արժեքների վրա։
11.E – `+=` չի կարող կիրառվել final փոփոխականի վրա։
12.A– Տռերնարի օպերատորը (`?:`) գործում է boolean արտահայտությունների վրա։
13.F – Ցիկլը չի աշխատում, քանի որ պայմանը սկզբից իսկ կեղծ է։
14.A,B – Ցիկլը կտպի 0, ապա 1։ ======  AD  
15.C,E,F – Բոլոր պայմաններն ունեն ճիշտ տրամաբանական արտահայտություններ։
16.C – while ցիկլը կշարունակի մինչև պայմանը true է։
17.F– Ցիկլը կտպի թվեր՝ 1-ից մինչև 5։
18.A, C, D, E – Տրված բոլոր ցիկլային գրառումները թույլատրելի են։
19.B,C – `continue` և `break` կառավարման հոսքի օպերատորներ են։
20.D – `i++` նախ օգտագործվում է, հետո միայն մեծացվում։
21.C – `while(true)` անվերջ ցիկլ է։
22.D – `for(int i = 0; i < 3; i++)` կկատարվի 3 անգամ։
23.A – `for(;;)` նույնպես անվերջ ցիկլ է։
24.A – `if (true)` պայմանը միշտ ճիշտ է։
25.D – Ցիկլը կտպի թվեր՝ 0-ից մինչև 2։
26.A,B,D – `for`, `while`, և `do-while` ցիկլներ են։
27. B – `++i` նախ մեծացնում է, հետո օգտագործում։
28. D,F – Հետպակասեցման դեպքում նախ արժեքը վերադարձվում է, հետո միայն պակասում։
29. B – Փոփոխականը նախ թարմացվում է։ =======D
30. F – Ճիշտ աշխատանքն ապահովում է միայն սինտաքսական ճիշտ գրառումը։
31. B – Ճիշտ օգտագործված է պայմանը։
32. E – Ճիշտ կիրառություն է տվյալ պարագայում։
33. B – Ճիշտ պատասխան՝ ըստ տրամաբանության։
